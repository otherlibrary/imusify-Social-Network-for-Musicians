import { EditFieldDirective } from './edit-field.directive';

describe('EditFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new EditFieldDirective();
    expect(directive).toBeTruthy();
  });
});
