import { AudioSliderDirective } from './audio-slider.directive';

describe('AudioSliderDirective', () => {
  it('should create an instance', () => {
    const directive = new AudioSliderDirective();
    expect(directive).toBeTruthy();
  });
});
